# Alongside Agent Instructions

The canonical agent instructions for this repository live in `AGENTS.md`.

Read and follow `AGENTS.md` for project layout, local development commands, verification, implementation conventions, and documentation rules. This file intentionally stays as a pointer so Claude and Codex use the same source of truth.

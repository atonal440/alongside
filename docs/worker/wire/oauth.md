# worker/src/wire/oauth.ts

OAuth wire schema scaffolding.

## Types And Schemas

Defines branded types and schemas for redirect URIs, client IDs, state, code challenge, code verifier, code challenge method, and token endpoint bodies.

The schemas are not wired into `worker/src/oauth.ts` yet.

# worker/src/wire/mcp.ts

MCP wire schema scaffolding.

## Exports

**`McpToolNameSchema`** — Branded tool-name parser.

**`McpToolCallParamsSchema`** — Initial schema for `tools/call` params.

**`McpToolName`** — Literal union of tool names from the shared enum registry.

This is a placeholder for the later typed MCP registry slice.

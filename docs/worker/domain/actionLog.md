# worker/src/domain/actionLog.ts

Domain action-log shapes and log policy scaffolding.

## Types

**`LogPolicy`** — Declares whether a tool requires a task ID, may log project/global work, or emits no log.

**`ActionLogEntryDomain`** — Branded action-log row shape for future codec work.

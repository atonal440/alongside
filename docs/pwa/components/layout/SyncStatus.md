# pwa/src/components/layout/SyncStatus.tsx

## Components

**`SyncStatus`** — Small status indicator displayed below the header. Shows "synced" when online and in sync, or "offline — changes saved locally" when the app cannot reach the server. Reads `syncStatus` from app state.

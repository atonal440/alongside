# pwa/src/components/layout/NavBar.tsx

## Components

**`NavBar`** — Horizontal tab bar with three navigation tabs: Suggest, All, and Session. Highlights the active tab based on `currentView` from app state. Clicking a tab dispatches `SET_VIEW` and calls `pushNav` to update browser history.

# pwa/src/components/common/EmptyState.tsx

## Components

**`EmptyState`** — Simple message component rendered when a list has no items to show (e.g. no active tasks in Session view, no tasks in a project group). Accepts a `message` prop.

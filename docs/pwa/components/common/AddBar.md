# pwa/src/components/common/AddBar.tsx

## Components

**`AddBar`** — A single-line text input with a submit button for creating new tasks. Calls `createTaskAction` on submit and clears the input. Used at the bottom of `AllView` and `SuggestView`.

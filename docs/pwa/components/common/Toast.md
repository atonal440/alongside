# pwa/src/components/common/Toast.tsx

## Components

**`Toast`** — Overlay notification that displays a short message string from `state.toast`. Auto-dismisses after 3 seconds by dispatching `SET_TOAST` with `null`. Rendered at the app-shell level so it floats above all views.

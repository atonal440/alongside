# pwa/src/main.tsx

Entry point for the React PWA. No exports.

Calls `ReactDOM.createRoot` on the `#root` div and renders `<App />`. This is the file Vite uses as its HTML entry point (`index.html` → `main.tsx`).

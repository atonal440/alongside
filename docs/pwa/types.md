# pwa/src/types.ts

Re-exports everything from `@shared/types` so PWA modules can import types from a single local path (`../types`) without needing to reference the `@shared` alias directly. No new types are defined here.
